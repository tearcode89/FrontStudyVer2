import {createTheme} from "@mui/material";

export const Mytheme = createTheme({
    palette: {
        type:'dark',
        primary: {
            main: '#2196f3',
        },
        secondary: {
            main: '#f50057',
        },
    },
});