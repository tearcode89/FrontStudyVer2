import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#ff0000', // 원하는 색상으로 변경
        },
        secondary: {
            main: '#00ff00', // 원하는 색상으로 변경
        },
    },
});

export default theme;
